<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hbq.cms.mapper.AreaMapper">
    <select id="findList" resultType="map">
        select
            t.id id,  -- id
            t.parent parent,  -- 父节点ID
            t.name name,  -- 名字
            t.level level,  -- 层级（0-全国 1-省 2-市 3-区）
            t.short_code shortCode,  -- 简称
            t.create_time createTime,  -- 创建时间
            t.update_time updateTime   -- 更新时间
        from area t
        <where>
            <if test="p.parent != null">
                and t.parent = #{p.parent}
            </if>
            <if test="p.name != null and p.name != ''">
                and t.name LIKE '%${p.name}%'
            </if>
            <if test="p.level != null">
                and t.level = #{p.level}
            </if>
            <if test="p.shortCode != null and p.shortCode != ''">
                and t.short_code LIKE '%${p.shortCode}%'
            </if>
            <if test="p.startTime != null and p.startTime != '' ">
                and t.create_time >= #{p.startTime}
            </if>
            <if test="p.endTime !=null and p.endTime != '' ">
                and #{p.endTime} >= t.create_time
            </if>
        </where>
        order by t.id desc
    </select>
</mapper>