<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hbq.cms.mapper.TaskMapper">
    <select id="findList" resultType="map">
        select
            t.id id,  -- 主键
            t.user_id userId,  -- 用户id
            t.title title,  -- 任务名
            t.start_time startTime,  -- 起始时间
            t.end_time endTime,  -- 终止时间
            t.status status,  -- 状态（0- 否 1-是）
            t.create_time createTime,  -- 创建时间
            t.update_time updateTime  -- 更新时间
        from task t
        where 1=1
        <if test="p.userId != null and p.userId != ''">
            and t.user_id LIKE '%${p.userId}%'
        </if>
        <if test="p.title != null and p.title != ''">
            and t.title LIKE '%${p.title}%'
        </if>
        <if test="p.startTime != null and p.startTime != ''">
            and t.start_time LIKE '%${p.startTime}%'
        </if>
        <if test="p.endTime != null and p.endTime != ''">
            and t.end_time LIKE '%${p.endTime}%'
        </if>
        <if test="p.status != null and p.status != ''">
            and t.status LIKE '%${p.status}%'
        </if>
        <if test="p.startTime != null and p.startTime != '' ">
            and t.create_time >= '${p.startTime}'
        </if>
        <if test="p.endTime !=null and p.endTime != '' ">
            and '${p.endTime}' >= t.create_time
        </if>
        order by t.id desc
    </select>
</mapper>